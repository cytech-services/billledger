# Optional compose overrides
# Copy to .env and adjust values as needed.

# Where to store persistent SQLite data (bills.db)
BILLLEDGER_DATA_DIR=./data

# Host port for frontend app
BILLLEDGER_PORT=3000
